robot_dimensions:
  # length and width on Studs
  length_x:
  width_y:

  # wheel diameter in mm
  wheel_diameter:

mat_dimensions:
  # length and with on mm
  length_x:
  width_y:

mat_image_path:

robot_motors:
  # one of (MEDIUM, LARGE)
  port_A:
  port_B:
  port_C:
  port_D:

robot sensors:
  # one of (GYRO, COLOR)
  port_1:
  port_2:
  port_3:
  port_4:

steps:
  delta_theta:
  delta_pixels:
  additional_motor_steps:
